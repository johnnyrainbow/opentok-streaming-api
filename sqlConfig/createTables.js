const Broadcast = require("../db/models/Broadcast")
Broadcast.createTable()

const Tutor = require("../db/models/Tutor")
Tutor.createTable()

const Room = require("../db/models/Room")
Room.createTable()

const StandardUser = require("../db/models/StandardUser")
StandardUser.createTable()